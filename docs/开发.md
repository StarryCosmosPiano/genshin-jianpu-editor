# 开发与验证

## 环境

- Node.js 20 或更高版本
- Tauri 桌面构建需要 Rust stable 与 Cargo
- 重新生成 JPW ANTLR parser 时需要 JDK
- 浏览器端截图回归使用本机 Microsoft Edge

首次安装使用锁定依赖：

```bash
npm ci
```

## 启动与构建

```bash
npm run dev          # Vite 开发服务器
npm run web          # Windows 本地网页端：127.0.0.1:1420 并自动打开
npm run build        # TypeScript 严格检查 + Vite 生产构建
npx tsc --noEmit     # 只检查 src/
npm run tauri dev    # Tauri 桌面应用
cd src-tauri && cargo check
```

根目录 `*-check.ts` 不在 `tsconfig.json` 的 `include` 中，由对应 npm 命令通过 esbuild 打包后执行。

## 回归层级

### 快速核心回归

```bash
npm run check:core
```

依次执行：

- `check:format`：共享扩展名注册、打开/保存信息、JPW/TXT 解析边界、多个 Voice 和乱序声部源码映射
- `check:horizontal`：节奏列、小节宽度、目标小节数和贪心装行
- `check:piano`：钢琴双行、总谱、MusicXML、花括号、小节线和 MIDI 结构
- `check:midi`：SMF 解析、量化、三连音、手部、多轨和速度
- `check:slashscore`：键盘谱/数字谱、时值、休止、U+2063 和多声部往返
- `check:source-mapping`：同音重复、琶音、局部换调、多声部三连音及长谱的音符到源码映射
- `check:incremental-layout`：模型快照的精确失效与新对象地址重绑；增量/完整排版对比，涵盖改音、时值、跨小节延音、三连音、页高/宽、样式、增删小节、调号、速度及失败后恢复
- `check:input:unit`：输入编辑纯逻辑、精确换调、三连音空格与延音、文档设置撤销和谱末光标边界；包含 `check:triplet` 的附点音去点创建、正拍混合元数据、第三格编辑及 TXT 往返，同小节独立组及重叠长短组的元数据隔离；跨拍长三连音的按拍输出、纯正文及旧版注解兼容回归
- `check:triplet:overlap`（随 `check:triplet` 运行）：`triplet-delimiter-check.ts` 核对同功能多种括号的规范输出及未改拍组原文保留；`overlapping-tuplet-check.ts` 核对不同声部重叠三连音分别恢复真实起点/时值、同显示格异时音不合成和弦；`overlapping-mordent-check.ts` 核对共享三连音内另一声部波音的 `[ABA]` 正文、主音独占普通时值和重复保存重载。同声部三连音成员仍不能添加波音。

### 浏览器端回归

```bash
npm run check:browser
# check:e2e 是同一命令的别名
```

该命令先构建一次，再执行：

- `selection-shot.mjs`：点选、框选、文本同步、方向键编辑、格式切换、拖拽
- `input-mode-check.mjs`：浏览器节奏命中、三连音、光标、和弦与右键记号往返
- `vertical-chord-input-check.mjs`：双谱行真实键盘连续向下/向上添加和弦音，检查每次虚线占位、声部/拍位保持、相邻声部不变、撤销重做及未填占位跨声部导航；支持 `APP_TEST_URL` 验证线上发布，纳入 `check:input` 与 `check:browser`。
- `input-picking-check.mjs`：在 0.6×、1×、1.6× 缩放下逐一点击和弦各音、三连音成员和组外普通音，核对普通选择与打谱模式均命中实际字形、音高、源码和真实起点，点击不改写正文
- `picking-check.mjs`：独立检查字形紧边界、和弦各音、波音与三连音数字的精确几何命中；SVG 字体框空白、空心括号曲线内部和页面空白不能误选对象
- `engraving-defaults-check.mjs`：旧浏览器的花括号默认值成对迁移、自定义值保留、版本标记与保存重载、实际花括号路径；可用 `APP_TEST_URL` 验证已部署的 GitHub Pages。`panel-interaction-check.mjs` 同样支持该环境变量，用真实滚轮验证折叠后视野保持、重新展开及调整浮窗大小后的滚动恢复。
- `symbol-picking-check.mjs`：从 Bravura 字形的独立光栅笔画像素定位可见符号，真实点击三连音数字、上/下波音、Tr 与延长号，覆盖 0.6×／1×／1.6× 缩放和普通/打谱模式，并拒绝旧倒置点击框的错误位置；不以透明点击框中心自证正确。`painter.ts` 在拾取边界统一转换 SMuFL 向上的 Y 轴为 SVG 向下的 Y 轴，保留原符号绘制位置与排版。
- `src/editor/app.ts`：普通点击、打谱点击和双击统一使用精确 picker；3 屏幕像素容差按 zoom 换算。输入模式仅在三连音范围内的空白按组内 ruler 吸附，直接点击音符不受附近刻度牵引
- `voice-selection-shot.mjs`：Alt+1～Alt+9、多声部和隐形标记
- `notation-flow-check.mjs`：声部分配/数量/格式切换撤销重做、谱末移动、乱序声部实际打谱
- `dotted-triplet-check.mjs`：双声部附点八分创建十六分三连音、第三格填入/替换/删除及撤销重做，验证正拍声部和组外休止不变
- `triplet-resize-check.mjs`：首成员改成八分后的混合括号、正拍时值、末成员音符/休止以及撤销重做；`check:triplet` 另覆盖 32 分正拍位置的重复保存重载
- `long-triplet-check.mjs`：二分/全音符创建三连音跨 2/4 个拍组、并行正拍和和弦不变、第二/第三成员输入与重载；同时核对全音符长组 SVG 只显示 `5 0 0`、每个实际 member 一个输入 anchor 且 `end` 完整；`check:triplet` 同时覆盖单声部、非零起点及错误后缀诊断
- `overlapping-tuplet-check.mjs`：在真实谱面逐声部创建可重叠三连音，核对各自真实时钟、括号和原有圆括号、时值诊断、撤销重做及重载；同格但实际起点不同的音不能合成和弦
- `src/layout/layout.ts`：不同声部三连音只有实际起止都相同才复用一组可见 `3` 括号；部分重叠或相互包含时，各自显示括号，且成员真实时值不因显示位置合并
- `overlapping-mordent-check.mjs`：在共享三连音内给另一声部的普通音右键添加上/下波音，核对 `[ABA]`、主音的源码定位与普通时值、三连音成员不变及保存重载
- `src/slashscore/beat-sliced-tuplet.ts`：跨拍 `scope=voice` 长三连音的拍切片恢复；导出 footer 的 `triplet.beatSlices=true` 作为稳定组 ID/真实时值恢复标记，旧巨括号 metadata 继续兼容
- `long-triplet-check.ts` / `long-triplet-check.mjs`：补充无 footer、按拍 `[]` 正文、leading bracket dots、普通声部最近细格合入及旧 metadata 兼容覆盖
- `midi-shot.mjs`：MIDI 导入对话框与网页预览
- `abc-check.mjs`：ABC→MusicXML 转换回归
- `painter-cache-check.mjs`：页面 SVG 与子节点复用、新模型点选/播放反查、颜色及旧高亮清理、结构增减和独立导出
- `input-entry-check.mjs`：进入打谱跟随谱面/文本活动位置，多选和弦、灰色延音、三连音、待解析文本、无选择等待、换文档与视野保持
- `floating-inspector-check.mjs`：悬浮窗口拖动/缩放/复位、边界与存储、非模态点谱及金色高亮对比
- `app-dialog-check.mjs`：页面内输入与确认窗口、速度/渐变/倚音、校验/取消/撤销、拖动与焦点、切文档取消旧输入、设置三种退出选择、窄屏边界及导出错误；不应触发浏览器原生提示框
- `keyboard-note-edit-check.mjs`：N 进入/退出打谱与按键显示冲突规则、21 琴键改音、和弦单音时值保留、大小写、修饰键、文本焦点和撤销
- `key-circle-check.mjs`：五度圈 C 顶端、15 调/升降数量、键盘导航、窄屏、调号/拍号浮窗及换文档关闭
- `panel-interaction-check.mjs`：花括号默认值/范围、细线默认、复选框对齐、靠底部分类折叠/展开保持视野与上滚回收留白
- `score-header-check.mjs`：JPW/TXT 谱头标题署名及调拍速度编辑、取消/撤销/重做、乐谱设置同步及切文档取消旧输入
- `inspector-check.mjs`：当前谱面 200ms 合并预览、草稿与保存值分离、立即应用、取消、未应用修改的面板切换与文档替换清理
- `theme-check.mjs`：浅色/深色/跟随系统、主题不解析或重建编辑器、白色纸张、桌面/小窗口/窄屏布局与截图（`artifacts/ui/`）
- `workspace-preferences-check.mjs`：默认键盘谱与文本、左侧样张保留编辑器、设置持久化、固定播放按钮及翻页防回闪
- `shortcut-preferences-check.mjs` / `check:shortcuts`：快捷键录入、冲突、保存取消恢复、场景分发、隐形标记原子性
- `ui-polish-check.mjs`：菜单内下拉框不关闭外层菜单、按钮悬停/焦点对比度、紫色选项与金色动作、附点/锁定/打谱状态、原琴 logo、面板内边距与样张放大
- `space-playback-check.mjs`：普通模式选音后空格从当前位置连续播放/停止；长按忽略重复；打谱仍前进、文本仍输入空格，覆盖 JPW/键盘谱/数字谱

界面专项可单独运行 `npm run check:ui`。此命令先构建，再运行打谱进入、面板草稿、主题、配色、播放、工作区设置、快捷键及悬浮输入/确认窗口专项。

### 界面组织与设置

主界面名称为“原琴助手编辑器”，保留 Tauri identifier、文件关联、旧存储键和文档格式。
`src/bootstrap/workspace.ts` 负责菜单、工具栏、文本面板拖动及状态栏；`src/ui/theme.ts` 负责主题，
`src/ui/inspector.ts` 负责单个悬浮设置窗口与未应用修改提示；`src/ui/floating-window.ts` 管理拖动、缩放、边界与独立位置存储。新增组织逻辑不放进 App 的编辑算法。

`src/ui/app-dialog.ts` 统一异步输入、确认和消息提示，替代 `window.prompt/confirm/alert`；文件选择与打印仍走原平台入口。
输入窗口复用 `FloatingWindow` 的可选尺寸、居中与不持久化配置；乐谱/排版窗口的默认尺寸及存储规则不变。
同一时间只显示一个输入提示，背景暂时 `inert`，关闭恢复焦点及原状态；Enter/Escape、Tab 焦点循环和多行 Ctrl/Cmd+Enter 在窗口内处理。
输入通过校验并确认后才走原编辑事务；取消和文档替换不提交，跨 await 的模型修改同时检查 Score 身份。
设置点击背景时异步提供应用/放弃/继续编辑三种结果，渐快/渐慢的速度与终点合并填写。拖动/缩放不解析或序列化乐谱。

`editor/score-header.ts` 组织谱头输入，App 仅协调原文档事务；`editor:header-changed` 用局部字段同步已打开的乐谱设置，不覆盖其他分类草稿。
`editor/keyboard-note.ts` 提供三排琴键映射；直接改音只替换活动音符的源码跨度，保留时值、和弦其他音和隐形声部标记。
`ui/details-scroll-retention.ts` 在分类折叠前预留空间，完成展开/折叠后恢复原 scrollTop；空间只随上滚回收，关闭面板解除监听。
花括号新默认宽0.35×、粗3.2px，滑条分别为0.1–2×、0.1–5px；旧版更大的自定义值仍由归一化保留。延音/圆滑线默认厚度4→3.2，几何算法不变。

主题存储在 `jpeditor.ui.theme`，默认 `system`；新版布局状态沿用 `jpeditor-render-settings`，
增加 `workspaceLayoutVersion: 2`、`codePaneWidth`、`beatPositionFormat`、`showTextOnStartup` 和 `restoreLastFileOnStartup`。
旧版迁移默认左侧展开，启动默认载入内置键盘 TXT 示例；可在设置选择启动时隐藏文本或恢复上次文件。宽度/左右偏好持久化。以上设置不写入乐谱。

排版草稿使用 `App.setEngravingPreview`：只针对当前 Score 重新排版，保留 `.Layout` 断行设置，
不解析、不序列化、不写存储。200ms 连续修改只预览最后一份草稿；应用同步提交并保存，取消清除待执行预览后恢复。
`setEngravingStyle` 提交已保存值，文档语义设置仍通过原有撤销事务提交。
`ui/layout-preview-pane.ts` 在左栏切换文本/样张，保留 CodeMirror 实例；排版关闭后恢复原文。
`editor/shortcuts.ts` 定义与缓存场景快捷键，`ui/shortcut-settings.ts` 提供独立编辑窗口，偏好位于 `jpeditor.ui.shortcuts.v1`。
花括号使用 `layout/brace.ts` 的填充路径；`EngravingStyle.connectBarlines` 默认 false，由设置控制钢琴/总谱跨行连接。
打谱进入位置由 `input-entry.ts` 解析，`ScoreInputSession` 支持已启用但 cursor 为 null 的等待状态。

### 完整回归

```bash
npm run check:all
```

等价于 `check:core` 加 `check:browser`。无头浏览器脚本使用 Playwright 的本地 Edge channel，
不会下载 Chromium。

### 长谱编辑性能

```bash
npm run check:performance
```

使用本机 Edge 测量 32、128、256 小节双声部键盘谱的输入、移动音符、延长音符及光标导航。
检查每次编辑只重排一次、光标导航不重新解析、屏外页面不被逐页测量、单小节编辑只重建相关钢琴系统；
实际键盘连续输入并撤销重做后，将保留的全部 SVG 与同一模型完整排版的结果逐节点比较。记录解析与排版所在的
`reload`、SVG 创建和光标绘制等耗时。毫秒数用于同机前后比较，不作为跨机器的硬性门槛。
已有构建时可直接执行 `node notation-performance-check.mjs`；可用 `PERF_MEASURES` 指定逗号分隔的小节数。
完整报告默认写入系统临时目录的 `notation-performance.json`，可用 `PERF_REPORT` 更改文件名。

## 按改动选择验证

| 改动区域 | 最低验证 |
|---|---|
| `editor/file-format.ts`、打开/保存/拖拽 | `check:format`、`check:selection` |
| JPW parser/import/serialize | `check:piano`、`check:selection` |
| 打谱模式、光标、右键记号 | `check:input`、`check:piano` |
| 键盘谱/数字谱、U+2063、时值 | `check:format`、`check:slashscore`、`check:voice` |
| MIDI | `check:midi`、`midi-shot.mjs` |
| 横向排版 | `check:horizontal`、`check:piano` |
| SVG、分页、钢琴/总谱 | `check:piano`、`shot.mjs`、`selection-shot.mjs` |
| MusicXML | `check:piano`、`shot.mjs --xml <path>` |
| ABC | `abc-check.mjs`、`abc-shot.mjs` |
| OMR | `shot-recog.mjs`；有本地 `testdata/` 时再跑 `measure-*` / `bench-*` |
| Tauri Rust | `npm run build`、`cd src-tauri && cargo check` |

## 截图与专项工具

```bash
node shot.mjs out.png
node shot.mjs out.png --xml path/to/file.musicxml
node abc-shot.mjs path/to/file.abc out.png
node slashscore-shot.mjs
node shot-recog.mjs
```

OMR benchmark 依赖未提交的本地 `testdata/`。Gemini OMR 另外依赖系统 PATH 中的 `agy`；
缺少这些可选资源不影响普通构建和核心回归。

## GitHub 发布

本项目的发布仓库为 `StarryCosmosPiano/genshin-jianpu-editor`，所有 `gh` 发布和工作流命令显式指定
`--repo StarryCosmosPiano/genshin-jianpu-editor`，避免被 `upstream` 远程影响。

从应用版本 0.4.0 起，使用 `yuanqin-v<版本>` 标签；仓库仍保留上游同名 `v0.4.x` 等历史标签，不移动或覆盖它们。
发布前先查询远端标签，确认新标签尚不存在；在验证完成的提交上创建并推送标签，使用
`gh release create <标签> --verify-tag --notes-file <说明文件> --repo StarryCosmosPiano/genshin-jianpu-editor`
创建发布。应用的 package、npm 锁文件、Tauri 配置、Cargo 包和锁文件版本保持一致。

`main` 推送部署在线版；Release 发布触发 Windows x64 和 macOS Apple Silicon 安装包构建。
检查工作流的提交与发布标签一致，并确认两个安装包均上传成功。核心回归必须只依赖已提交文件或内置样本；
本机私有曲谱不能成为 CI 必需条件。现有 `scripts/release.sh` 使用旧标签规则和 macOS 命令，不能直接用于此发布流程。

用户明确要求更新同一应用版本时，保留现有 Release，先完成验证并推送 `main`；仅更新该应用的 `yuanqin-v<版本>` 标签，并用远端旧标签对象的 `--force-with-lease` 防止覆盖他人的更新。随后手动运行 `Build` 工作流并传入 `release_tag`，工作流校验标签与应用版本一致、查找现有 Release，再构建并替换对应安装包；不会重新创建发布或覆盖说明。手动输入留空仍仅部署 Pages。安装包统一使用 `Yuanqin.Editor_[version]_[arch][setup][ext]` 文件名，保持同版本下载地址不变。

## 修改约定

- 工作区可能包含用户未提交改动，禁止用 `git reset --hard` 或覆盖无关文件。
- 文件按单个文件依次保存，避免一次机械重写多个核心文件。
- `src/jpword/parser/` 是生成代码，修改 `Jpwabc.g4` 后重新生成。
- 排版测量必须继续使用 `common/measure.ts` 的 SVG 测量路径。
- 不在源码中写 SMuFL 字面 PUA 字符。
- 本地 `src/assets/soundfonts/*.sf2`、`testdata/` 和用户曲目由 Git 忽略，不进入发行包。

项目模块和数据流见 [技术栈.md](技术栈.md)，当前功能状态见 [进度.md](进度.md)。所有简谱行为及其稳定逻辑 ID、实现入口和最低回归集中登记在 [简谱逻辑总表.md](简谱逻辑总表.md)；行为改动必须同步更新对应行，便于用 Git diff 查看本次改了哪些逻辑。

悬浮窗口偏好保存为 `jpeditor:inspector-window:score:v1` 与 `jpeditor:inspector-window:layout:v1`，与乐谱及排版参数分开。
谱面选音两主题统一使用 `--score-selection-ink: #A98DD4`；文本保持 `--selection-ink: #EFBB52`，按钮仍使用原品牌金色；关联文本的实底金色和深色字只作用于临时选区，取消后恢复语法/声部色。
