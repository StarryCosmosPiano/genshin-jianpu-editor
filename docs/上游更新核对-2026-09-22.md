# 上游 jpeditor 更新核对（2026-09-22）

## 核对范围

- 本地 HEAD：`41a1dc7`，tag `v0.3.1`，2026-08-14；工作区本来就有大量未提交修改，本次没有改动、暂存或清理它们。
- upstream：`https://github.com/lodebar2026/jpeditor.git`，执行了 `git fetch upstream --prune`，当前 `upstream/main=f19dc6b`、tag `v0.7.4`，2026-09-22。
- `git rev-list --left-right --count HEAD...upstream/main` 返回 `157 686`，且 Git 报告 `HEAD...upstream/main: no merge base`。这说明上游已经重写/迁移历史；技术上仍可对单个提交尝试 cherry-pick，但不能把 upstream 提交当作当前 fork 的可直接同步序列，冲突和语义错配风险很高。

GitHub 页面截至核对时显示上游默认分支为 `main`，最新 tag 为 v0.7.4，历史 665 个提交；仓库页面列出的能力包括可视化编辑、文本谱、MusicXML、OMR、ABC、播放和导出：[上游仓库](https://github.com/lodebar2026/jpeditor)。

## 关键差异证据

上游 v0.7.4 的 `package.json` 已改为 `jpeditor` 0.7.4，增加 `src/cli`、`src/model`、`src/pu`、`src/j123` 相关构建与方言能力，并引入 `opencc-js`、`jspdf`、`sharp`、`pdf-lib` 等依赖；本地仍是 `genshin-jianpu-editor` 0.3.1，保留原有 `slashscore`、回归脚本和 `tesseract.js`。这不是同一依赖/目录拓扑上的小版本更新。

上游近期提交集中在已经迁移后的新模块：

| 提交 | 变化 | 影响判断 |
|---|---|---|
| `f19dc6b` | v0.7.4 版本号与 Tauri 锁文件 | 只能作为上游发布标记，不能单独合入 |
| `5e45b6e` | 新增 `src/model/beatcheck.ts`、`src/omr/beats.ts`，编辑器与 OMR 小节时值自检 | 有价值，但依赖上游 model/visual/OMR 拓扑；直接移植会改诊断与样式 |
| `85b7a48` | 新增 `src/common/lines.ts`，123/诗歌本解析按真实 CRLF 偏移 | 逻辑上低风险，但本地没有 `src/j123`、`src/pu` 解析器，无法直接套用；可作为当前源码定位回归的参考 |
| `6fee412` | `src/editor/visual/ops.ts` 修正增时线不并入选区 | 本地没有 `src/editor/visual`，不可直接 cherry-pick；若未来移植可单独补等价 CodeMirror 选区测试 |
| `20fd74a` | 弱起/删空小节时值诊断与点击跳转 | 依赖上游 `beatcheck` 和 visual controller；当前本地三连音、弱起逻辑不同，直接合入有误报风险 |
| `7c16b47`、`38cf6f5` | 新右键记号面板、按钮改为谱面符号，并改 `src/styles.css` | 明确会影响当前工具栏/样式；不建议合入 |
| `ddf776c` | 新 `src/model/alignparts.ts`，多声部补无声小节并共用试听/MusicXML 导出 | 与当前 Part/Score、`timeline.ts`、MusicXML 导出有关，但实现不兼容；需要以当前多声部回归重新设计，不能搬提交 |
| `266bd25` | OMR 空心楔形重音识别，写成 `!accent!`/`&zy` | 只影响 OMR 图像识别与文本输出；本地 `src/omr/jianpu.ts` 版本/模型接口不同，需独立移植和样本回归 |

## 已有或重复修复

上游 `ad22bdb` 的 MusicXML 多行 `<credit-words>` 修复在当前本地历史中已有等价提交 `13b81bf`，当前 `src/score/musicxml.ts` 已保留多行合并逻辑。因此不应重复移植。

## 结论

当前没有可安全直接合入、同时保证现有样式和打谱行为不变的 upstream commit。最值得后续“按功能重写移植”的项目是：

1. `85b7a48` 的 CRLF SourceSpan 偏移修复，前提是先把逻辑对接到当前 `.jpwabc`/TXT 解析入口并补 CRLF 选择回归。
2. `5e45b6e` 的小节时值自检，前提是映射到当前 `Score`/`Measure`/诊断高亮，不直接引入上游 visual controller。
3. `ddf776c` 的多声部对齐思想，前提是与当前 `timeline.ts`、Part 声部、MusicXML 导出及正在修复的三连音行为一起设计。
4. `266bd25` 的 OMR 重音几何判据，前提是用当前测试图和当前 PaddleOCR/`musicpp` 管线验证。

`6fee412`、`7c16b47`、`38cf6f5` 属于上游可视化编辑系统的一部分，直接移植会触碰样式和交互，不列为当前“无样式影响”候选。建议后续采用上游功能说明与小范围 diff 作为参考，在当前架构中重写并逐项回归，而不是合并 upstream 分支。
